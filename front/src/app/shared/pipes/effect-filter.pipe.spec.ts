import { EffectFilterPipe } from './effect-filter.pipe';

describe('EffectFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EffectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

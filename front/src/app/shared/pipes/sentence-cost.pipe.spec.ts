import { SentenceCostPipe } from './sentence-cost.pipe';

describe('SentenceCostPipe', () => {
  it('create an instance', () => {
    const pipe = new SentenceCostPipe();
    expect(pipe).toBeTruthy();
  });
});

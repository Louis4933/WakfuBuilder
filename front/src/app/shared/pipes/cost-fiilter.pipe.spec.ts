import { CostFiilterPipe } from './cost-fiilter.pipe';

describe('CostFiilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CostFiilterPipe();
    expect(pipe).toBeTruthy();
  });
});
